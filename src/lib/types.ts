// Re-exports the desktop app's own type definitions under the same "@/lib/types" import path the
// ported web-app files (AlbumSpreadCanvasEditor.tsx, albumGrid.ts, albumTemplateBank.ts) use — so
// those files can be copied over unmodified when the web app's originals change.
export * from "../types";
